---
import { Image } from "astro:assets";

interface props {
  src: string;
  alt: string;
}

const { src, alt } = Astro.props;
---

<Image
  src={src}
  alt={alt}
  class="md:w-[400px] w-[350px] h-[250px] md:h-[350px] rounded-lg object-cover"
/>
