---
import { Image } from "astro:assets";

interface props {
  src: string;
  alt: string;
}

const { src, alt } = Astro.props;
---

<div>
  <Image
    src={src}
    alt={alt}
    class="h-auto max-w-[400px] rounded-lg md:w-[250px]"
  />
</div>
